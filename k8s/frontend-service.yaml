apiVersion: v1
kind: Service
metadata:
  name: movie-frontend-service
spec:
  selector:
    app: movie-frontend
  ports:
  - protocol: TCP
    port: 5173
    targetPort: 5173
    nodePort: 30816
  type: NodePort
